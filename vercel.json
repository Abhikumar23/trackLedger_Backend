{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "controllers/**",
          "database/**",
          "middleware/**",
          "model/**",
          "routes/**",
          "uploads/**",
          "utils/**"
        ]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "server.js" }
  ]
}
